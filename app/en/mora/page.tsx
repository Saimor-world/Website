import MoraProductPage from '@/components/MoraProductPage';

export const metadata = {
  title: 'Môra – Context, memory and action',
  description: 'Môra is Saimôr’s traceable context and agent layer: dedicated memory, transparent model connections and prepared actions.',
};

export default function MoraPage() {
  return <MoraProductPage locale="en" />;
}
